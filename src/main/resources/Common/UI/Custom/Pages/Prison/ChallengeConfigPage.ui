$C = "../../Common.ui";

$C.@PageOverlay {
    $C.@Container {
        Anchor: (Width: 1200, Height: 850);

        #Title {
            Group {
                $C.@Title {
                    @Text = "CONFIGURATION CHALLENGES";
                }
            }
        }

        #Content {
            LayoutMode: Top;

            // Header bar
            Group #HeaderBar {
                Anchor: (Height: 45);
                Background: (Color: #16212f);
                LayoutMode: Left;
                Padding: (Horizontal: 15);

                Label #HeaderTitle {
                    FlexWeight: 1;
                    Text: "CHALLENGES ADMIN";
                    Style: (FontSize: 14, TextColor: #ffd700, RenderBold: true, RenderUppercase: true, HorizontalAlignment: Center, VerticalAlignment: Center);
                }

                TextButton #CloseBtn {
                    Anchor: (Width: 40, Height: 36);
                    Text: "X";
                    Style: TextButtonStyle(
                        Default: (Background: #2a1a1a, LabelStyle: (FontSize: 16, TextColor: #ff4444, VerticalAlignment: Center, RenderBold: true)),
                        Hovered: (Background: #3a2a2a, LabelStyle: (FontSize: 16, TextColor: #ff6666, VerticalAlignment: Center, RenderBold: true)),
                        Sounds: $C.@ButtonSounds
                    );
                }
            }

            // Content area
            Group #PageContent {
                FlexWeight: 1;
                LayoutMode: TopScrolling;
                ScrollbarStyle: $C.@DefaultScrollbarStyle;
                Padding: (Full: 15);
            }
        }
    }
}
